name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run pytest
        run: pytest -v

  lint:
    name: Run Linter
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run flake8
        run: flake8 app tests

  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: [test, lint]
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build Docker image
        run: docker build -t mortgage-app:latest .

  deploy:
    name: Deploy to Production
    runs-on: self-hosted
    needs: [build]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4
      
      - name: Build Docker image on production
        shell: powershell
        run: docker build -t mortgage-app:latest .
      
      - name: Stop and remove old container
        shell: powershell
        run: |
          # Останавливаем контейнер, если он существует
          $containerId = docker ps -q -f name=mortgage-app
          if ($containerId) {
            docker stop mortgage-app
          } else {
            Write-Host "Контейнер mortgage-app не запущен"
          }
          
          # Удаляем контейнер, если он существует
          $containerIdAll = docker ps -aq -f name=mortgage-app
          if ($containerIdAll) {
            docker rm mortgage-app
          } else {
            Write-Host "Контейнер mortgage-app не существует"
          }
      
      - name: Run new container
        shell: powershell
        run: |
          docker run -d --name mortgage-app -p 8000:8000 --restart unless-stopped mortgage-app:latest